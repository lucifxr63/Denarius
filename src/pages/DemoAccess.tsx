import { useState } from 'react';
import { Link, Navigate, useParams } from 'react-router-dom';
import { ArrowRight, LoaderCircle, PlayCircle, ShieldCheck } from 'lucide-react';
import { toast } from 'sonner';
import { resetDenariusDemoPyme, resetDenariusDemoStartup } from '@/lib/queries';

export function DemoAccess(){const{model}=useParams();const[busy,setBusy]=useState(false);if(model!=='pyme'&&model!=='startup')return <Navigate to="/learn" replace/>;const startup=model==='startup';async function start(){setBusy(true);try{await(startup?resetDenariusDemoStartup():resetDenariusDemoPyme());toast.success('Tu demo personal está lista');window.location.assign('/dashboard')}catch(error){toast.error(error instanceof Error?error.message:'No pudimos preparar la demo.');setBusy(false)}}return <main className="grid min-h-[calc(100vh-4rem)] place-items-center px-4 py-10"><section className="w-full max-w-2xl rounded-3xl border border-primary/25 bg-card p-6 shadow-xl sm:p-9"><p className="flex items-center gap-2 text-sm font-semibold text-primary"><PlayCircle className="size-4" aria-hidden="true"/>Demostración guiada · datos ficticios</p><h1 className="mt-3 font-display text-3xl font-bold">{startup?'Nébula SaaS':'Comercial Andes'}</h1><p className="mt-3 leading-7 text-muted-foreground">{startup?'Decide si puedes crecer sin comprometer el runway.':'Protege la caja y ordena la cobranza de esta semana.'}</p><div className="mt-6 rounded-2xl border border-border bg-background/60 p-5"><h2 className="font-semibold">Qué sucederá al comenzar</h2><ul className="mt-3 grid gap-2 text-sm text-muted-foreground"><li>• Crearemos una copia aislada en tu cuenta.</li><li>• Podrás editarla y completar el happy flow.</li><li>• No se modificará ninguna empresa real.</li></ul></div><div className="mt-6 flex flex-col gap-3 sm:flex-row"><button type="button" disabled={busy} onClick={()=>void start()} className="inline-flex min-h-11 flex-1 cursor-pointer items-center justify-center gap-2 rounded-xl bg-primary px-5 text-sm font-semibold text-primary-foreground transition-colors hover:bg-primary/90 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-primary/60 disabled:cursor-wait disabled:opacity-60">{busy?<LoaderCircle className="size-4 animate-spin"/>:<ArrowRight className="size-4"/>}{busy?'Preparando tu copia…':'Comenzar demo'}</button><Link to="/learn" className="inline-flex min-h-11 items-center justify-center rounded-xl border border-border px-5 text-sm font-semibold hover:bg-muted">Ver los tutoriales</Link></div><p className="mt-5 flex gap-2 text-xs leading-5 text-muted-foreground"><ShieldCheck className="mt-0.5 size-4 shrink-0 text-primary"/>El enlace comparte el escenario, nunca información financiera ni acceso de otra persona.</p></section></main>}
