import test from'node:test';import assert from'node:assert/strict';import fs from'node:fs';const read=(p:string)=>fs.readFileSync(new URL(`../${p}`,import.meta.url),'utf8');test('shared demo routes and learning exist',()=>{const app=read('src/App.tsx'),access=read('src/pages/DemoAccess.tsx'),learn=read('src/pages/LearningCenter.tsx');assert.match(app,/\/demo\/:model/);assert.match(app,/\/learn/);assert.match(access,/resetDenariusDemoPyme/);assert.match(access,/resetDenariusDemoStartup/);assert.match(access,/copia aislada/);assert.match(learn,/Dueño o gerente de PyME/);assert.match(learn,/Founder o CEO de Startup/)});test('database change preserves authenticated ownership',()=>{const sql=read('supabase/migrations/20260810190000_den122_shareable_demo_beta_learning.sql');assert.match(sql,/auth\.uid\(\)/);assert.match(sql,/authenticated closed-beta user/);assert.doesNotMatch(sql,/grant execute.*anon/i)});
