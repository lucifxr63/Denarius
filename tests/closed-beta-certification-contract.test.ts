import test from'node:test';import assert from'node:assert/strict';import fs from'node:fs';const read=(p:string)=>fs.readFileSync(new URL(`../${p}`,import.meta.url),'utf8');
test('beta gate covers every Denarius role',()=>{const script=read('launch/integration-test-role-matrix.mjs');for(const role of['finance_manager','operator','viewer','advisor'])assert.match(script,new RegExp(role));assert.match(script,/owner administra el equipo completo/)});
test('role certification proves cross-tenant denial and zero-trace cleanup',()=>{const script=read('launch/integration-test-role-matrix.mjs');assert.match(script,/no cruza a otra empresa/);assert.match(script,/admin\.auth\.admin\.deleteUser/);assert.match(script,/cero huella/)});
test('beta epic forbids shared-database destructive changes',()=>{const epic=read('docs/backlog/EPIC-DEN-130-closed-beta-certification.md');assert.match(epic,/Validus|base compartida/);assert.match(epic,/No se acepta/);assert.match(epic,/rollback no destructivo/)});
test('invite tokens use the explicit extensions schema',()=>{const sql=read('supabase/migrations/20260810270000_den130_invite_token_fix.sql');assert.match(sql,/extensions\.gen_random_bytes/);assert.match(sql,/extensions\.digest/);assert.doesNotMatch(sql,/drop table|alter table/i)});
