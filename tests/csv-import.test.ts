import assert from 'node:assert/strict';import test from 'node:test';import {mapCsvRows,parseCsv,suggestMapping} from '../src/lib/csv-import.ts'
test('CSV reconoce delimitador y campos entre comillas',()=>{const parsed=parseCsv('fecha;monto;descripcion\n2026-08-01;-12.500;"Banco, comisión"');assert.equal(parsed.records[0].descripcion,'Banco, comisión');assert.equal(suggestMapping(parsed.headers).amount,'monto')})
test('mapea montos negativos como egresos y conserva referencia',()=>{const parsed=parseCsv('fecha,monto,glosa,referencia\n2026-08-01,-12500,Comisión,ABC');const result=mapCsvRows(parsed.records,{date:'fecha',amount:'monto',description:'glosa',reference:'referencia'});assert.deepEqual(result.valid[0],{date:'2026-08-01',amount:12500,type:'OUT',description:'Comisión',reference:'ABC'})})
test('rechaza filas inválidas antes de enviarlas',()=>{const parsed=parseCsv('fecha,monto\n01/08/2026,x');const result=mapCsvRows(parsed.records,{date:'fecha',amount:'monto'});assert.equal(result.valid.length,0);assert.equal(result.errors.length,1)})
