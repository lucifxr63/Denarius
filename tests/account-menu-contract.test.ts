import assert from'node:assert/strict';import fs from'node:fs';import test from'node:test';const menu=fs.readFileSync('src/components/layout/AccountMenu.tsx','utf8'),header=fs.readFileSync('src/components/layout/ProductHeader.tsx','utf8'),account=fs.readFileSync('src/pages/Account.tsx','utf8'),app=fs.readFileSync('src/App.tsx','utf8');
test('la cuenta esta disponible en la cabecera global',()=>{assert.match(header,/AccountMenu/);assert.match(menu,/Perfil/);assert.match(menu,/Configuración/);assert.match(menu,/Cerrar sesión/)});
test('el cierre de sesión maneja espera error y redireccion',()=>{assert.match(menu,/disabled=\{busy\}/);assert.match(menu,/await signOut\(\)/);assert.match(menu,/navigate\('\/login'/);assert.match(menu,/toast\.error/)});
test('el menu es tactil y operable con teclado',()=>{assert.match(menu,/min-h-11/);assert.match(menu,/aria-label="Abrir menú de usuario"/);assert.match(menu,/event\.key === 'Escape'/);assert.match(menu,/focus-visible:ring-2/)});
test('perfil y configuracion exponen identidad empresa y seguridad',()=>{assert.match(app,/path="\/account"/);assert.match(account,/id="profile"/);assert.match(account,/id="settings"/);assert.match(account,/Denarius no modifica este perfil/);assert.match(account,/Conexiones MCP/)});
