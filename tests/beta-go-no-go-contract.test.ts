import test from'node:test';import assert from'node:assert/strict';import fs from'node:fs';const source=fs.readFileSync(new URL('../scripts/certify-beta-go-no-go.mjs',import.meta.url),'utf8');test('final beta gate covers every certified block and is fail-fast',()=>{for(const block of['Roles y aislamiento','Happy flows PyME y Startup','Ciclo de equipo','Puerta operativa','Frescura de datos'])assert.match(source,new RegExp(block));assert.match(source,/if\(result\.status!==0\)break/);assert.match(source,/NO-GO BETA CERRADA/)});
