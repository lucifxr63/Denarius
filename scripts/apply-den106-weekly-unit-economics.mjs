import fs from'node:fs';const env=Object.fromEntries(fs.readFileSync('.env.local','utf8').split(/\r?\n/).filter(l=>/^\s*[A-Za-z_][A-Za-z0-9_]*\s*=/.test(l)).map(l=>{const i=l.indexOf('=');return[l.slice(0,i).trim(),l.slice(i+1).trim().replace(/^(['"])(.*)\1$/,'$2')]}));const token=process.env.SUPABASE_ACCESS_TOKEN||env.SUPABASE_ACCESS_TOKEN,ref=new URL(env.VITE_SUPABASE_URL).hostname.split('.')[0],query=fs.readFileSync('supabase/migrations/20260808100000_den106_weekly_close_unit_economics.sql','utf8'),response=await fetch(`https://api.supabase.com/v1/projects/${ref}/database/query`,{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify({query})});if(!response.ok)throw new Error(`${response.status}: ${await response.text()}`);console.log('APPLIED DEN-106 weekly close and unit economics')
