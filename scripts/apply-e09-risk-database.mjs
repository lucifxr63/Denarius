import fs from'node:fs';const env=Object.fromEntries(fs.readFileSync('.env.local','utf8').split(/\r?\n/).filter(x=>/^\s*[A-Za-z_][A-Za-z0-9_]*\s*=/.test(x)).map(x=>{const i=x.indexOf('=');return[x.slice(0,i).trim(),x.slice(i+1).trim().replace(/^(['"])(.*)\1$/,'$2')]}));const token=process.env.SUPABASE_ACCESS_TOKEN||env.SUPABASE_ACCESS_TOKEN;const ref=new URL(env.VITE_SUPABASE_URL).hostname.split('.')[0];const query=fs.readFileSync('supabase/migrations/20260807050000_saas_customer_risk.sql','utf8');const response=await fetch(`https://api.supabase.com/v1/projects/${ref}/database/query`,{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify({query})});if(!response.ok)throw new Error(`${response.status}: ${await response.text()}`);console.log('APPLIED E09 SaaS risk migration');
