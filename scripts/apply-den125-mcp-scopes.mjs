import fs from'node:fs';const e=Object.fromEntries(fs.readFileSync('.env.local','utf8').split(/\r?\n/).filter(l=>/^\s*[A-Za-z_][A-Za-z0-9_]*\s*=/.test(l)).map(l=>{const i=l.indexOf('=');return[l.slice(0,i).trim(),l.slice(i+1).trim().replace(/^(['"])(.*)\1$/,'$2')]})),token=process.env.SUPABASE_ACCESS_TOKEN||e.SUPABASE_ACCESS_TOKEN,ref=new URL(e.VITE_SUPABASE_URL).hostname.split('.')[0],query=fs.readFileSync('supabase/migrations/20260810220000_den125_mcp_scopes.sql','utf8'),r=await fetch(`https://api.supabase.com/v1/projects/${ref}/database/query`,{method:'POST',headers:{Authorization:`Bearer ${token}`,'Content-Type':'application/json'},body:JSON.stringify({query})});if(!r.ok)throw new Error(`${r.status}: ${await r.text()}`);console.log('APPLIED DEN-125 MCP scopes')
